function Q = Triquad(fun,X)

%
Q = (9/20)*fun(   (X(1,:) + X(2,:) + X(3,:))/3   ) + ...
    (1/20)*(  fun(X(1,:))  + fun(X(2,:)) + fun(X(3,:)) ) +...
    (2/15)*(  fun((X(1,:) + X(2,:))/2) + ...
              fun((X(2,:) + X(3,:))/2) + ...
              fun((X(3,:) + X(1,:))/2));


end

